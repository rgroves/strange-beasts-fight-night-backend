// non-deterministic
// This file contains shared code for the non-deterministic (ND) libraries that aren't allowed in workflows.
import path from 'path';

export const GAME_ASSETS_DIR = path.join(__dirname, 'server', 'public', 'game-assets');
